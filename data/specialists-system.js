// Specialists system data - temporary until we load from JSON
// This will be replaced with actual JSON loading in production

window.SpecialistData = {
    // This file serves as a bridge between the JSON data structure
    // and the frontend until we implement proper JSON loading
    loaded: true
};
